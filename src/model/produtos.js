const ProdutosM = (descricao, quantidade, valor, tipo) => {
    return {
        descricao: descricao,
        quantidade: quantidade,
        valor: valor,
        tipo: tipo
    }
}

export default ProdutosM